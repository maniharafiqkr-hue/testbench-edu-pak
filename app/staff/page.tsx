import Link from "next/link";
import { Brand } from "../components/Brand";

export default function StaffPage() {
  return (
    <main className="staff-page"><header className="staff-header container"><Brand /><div><span>Pilot Academic Lead</span><button className="avatar" aria-label="Open staff profile">MR</button></div></header><div className="container staff-title"><div><span className="eyebrow">TEACHER WORKSPACE</span><h1>Good evening, Ms. Rafiq.</h1><p>Three student responses need attention today.</p></div><Link className="button button-secondary" href="/">View student site</Link></div><div className="container staff-metrics"><article><span>Awaiting marking</span><strong>3</strong><small>Oldest · 3 hours</small></article><article><span>Returned today</span><strong>7</strong><small>Average · 8 min each</small></article><article><span>Content in review</span><strong>2</strong><small>No critical issues</small></article><article><span>Pilot completion</span><strong>68%</strong><small>17 of 25 students</small></article></div><section className="container marking-panel panel"><div className="panel-heading"><div><span className="card-kicker">MARKING QUEUE</span><h2>Extended responses</h2></div><button className="button button-small">Mark next response</button></div><div className="queue-table"><div className="queue-row queue-head"><span>Submission</span><span>Task</span><span>Received</span><span>Status</span></div>{[["TB-1048", "Narrative writing", "3 hours ago", "Due today"], ["TB-1051", "Précis & title", "48 min ago", "On time"], ["TB-1053", "Narrative rewrite", "12 min ago", "Rewrite"]].map((row) => <div className="queue-row" key={row[0]}>{row.map((cell, index) => <span key={cell} className={index === 3 ? "queue-status" : ""}>{cell}</span>)}</div>)}</div></section></main>
  );
}
