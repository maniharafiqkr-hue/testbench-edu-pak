# Neon pooled PostgreSQL connection string. Never commit a real credential.
DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require

# Private pilot code for the teacher marking workspace. Use at least 12 random characters.
TEACHER_ACCESS_CODE=replace-with-a-long-random-code
